@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-2">
    <div class="row pt-4">
        <div class="col-6">
            <h3>CoverType List</h3>
        </div>
        <div class="col-6 text-end ">
            <a class="btn btn-primary " asp-controller="CoverType" asp-action="Create">
                <i class="bi bi-person-plus-fill"></i> &nbsp; Create New CoverType
            </a>
        </div>
    </div>
    <br>
    <table class="table   table-striped " style="width:100%">
        <thead>
            <tr>
                <th class="h4 pt-4" style="text-align: center">
                    CoverType Name
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td width="60%" style="text-align: center">
                        <h5 class="pt-2"> @obj.Name</h5>
                    </td>
                    <td>
                        <a class="btn btn-primary " asp-controller="CoverType" asp-action="Edit" asp-route-id="@obj.Id">
                            <i class="bi bi-pen"></i> &nbsp; Edit
                        </a>
                        <a class="btn btn-danger " asp-controller="CoverType" asp-action="Delete" asp-route-id="@obj.Id">
                            <i class="bi bi-x-circle"></i> &nbsp; Delete
                        </a>
                    </td>
                </tr>
            }

        </tbody>

    </table>
</div>